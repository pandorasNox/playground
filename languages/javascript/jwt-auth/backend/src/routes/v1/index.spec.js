
const app = require('./../../../app');
const request = require('supertest');

describe('Test the index path', () => {
    //it should respond for GET method without param with redirect to an 404 page
    //it should respond for GET method with unknown hash param with redirect to an 404 page
    //it should respond for GET method with known param to redirect to related target url 
});
