
import React, { Component, Fragment } from 'react';
import { Default as DefaultTheme } from './../../themes';

const About = ({children}) => (
    <DefaultTheme><h1>About</h1>{children}</DefaultTheme>
);

export default About;
